# H3 vs 之前方法 · 视频生成路线比对

> 日期：2026-08-19 ｜ 目的：把 H3 多图参考生成和以往用过的视频生成路线放一起横向比较，决定以后哪条路该在什么时候用

---

## 一、横评总览

| 维度 | **H3 (Ref2VA, 本次)** | LTX-2.3 I2V (L5-3) | Vidu API (商用) |
|---|---|---|---|
| 平台 | AutoDL 租卡 + ComfyUI | AutoDL 租卡 + ComfyUI | Vidu 官方 API |
| 模型 | MiniMax H3 Ref2VA | LTX-2.3 22B | Vidu Q3 Pro |
| 参考输入 | **9图+3视频+3音频** | 1 首帧图 | 1-3 图 / 7 图(Q3) |
| 音频 | **原生生成**(对白/音效/配乐一次出) | 无(需后配音) | Q3 可音画同出 |
| 分辨率 | 864×480 (可更高) | 832×1216 竖屏 | 720P/1080P |
| 时长 | ~5s (124帧@24fps) | ~4s (97帧) | 1-16s |
| 单条耗时 | ~1.5-3min | 2-4min | 秒级~分钟级 |
| 成本 | 按租卡时间 ¥0.3/条级 | ¥0.24/条级 | 按次计费 |
| 一致性 | 多图锚定,强 | 单图起步,弱 | 多图参考强 |
| 关键瓶颈 | 显存 48G全量/32G量化 | 机械感/木偶感 | 需充值,贵 |

## 二、各路线怎么选（结论）

### 用 H3 的场景（推荐主力）
- 需要**一次出带声音的完整片段**（对白/音效/配乐不用后配）
- 需要**强角色/场景一致性**——有多张参考图/视频/音频时（短剧、系列角色）
- 想要"参考越多越稳"的多模态锚定（9图+3视频+3音频全开）

### 用 LTX-2.3 的场景
- 只要画面、不要声音，后期自己配音的流水线
- 竖屏短视频（832×1216 原生比 H3 的 864×480 更竖）
- 想要更高分辨率/竖屏构图时
- **坑**：固定 seed 会转圈、动作提示词要"动作导向"、机械感偏重

### 用 Vidu 的场景
- 商用交付（稳定、质量高、平台 SLA）
- 不想自己管 GPU/不想学 ComfyUI 时
- 需要 Q3 级 7 图参考或超长视频时
- **坑**：要充值、按次计费贵、参考图数量上限 3/7

## 三、H3 相对前两者的真正优势（一句话）

> **LTX 出"画面"，Vidu 出"视频"，H3 出"带声音的完整短剧片段"** ——
> 它把"参考一致性"和"原生音频"两个最费后期的事一次性做掉，
> 是三者里唯一一个图/视频/音频全模态参考的。

## 四、衔接进既有流水线

本地已有：
- 分镜管线 `pipeline/scripts/batch_generate.py`（读分镜JSON → 批量生成）
- 角色参考图 `comfyui/images/all_images/`（24张）+ LoRA `hongyi_lora.safetensors`
- LTX-2.3 5镜头成片 `pipeline/clips/`

改造方向：
1. 分镜 JSON 的每个 shot 增加 `ref_images: [图片路径]` 字段 → H3 R2V 批量出片
2. 角色一致性：用 `all_images` 的多张参考图做 H3 参考锚（替代/补充 LoRA）
3. 音频：H3 原生出音轨后，若效果达标可跳过 GPT-SoVITS 配音环节
4. 竖屏需求 → 调整 width/height 参数（H3 支持 32 步进的自定义分辨率）
