# MiniMax H3 多图参考生成视频 · 操作记录

> 日期：2026-08-19 ｜ 平台：AutoDL ｜ 模式：autodl-cli + ComfyUI 原生 API
> 产出：`output/r2v_test_00001.mp4`（单图参考）+ `output/r2v_multi_2img.mp4`（双图参考）
> 参考图：本地 `comfyui/images/all_images/char_ref_00001~00002.png`（以前训练 LoRA 用的角色参考图）

---

## 一、本次做了什么

在 AutoDL 租 vGPU-32G 实例（RTX 4080，¥1.77/时），用预装 MiniMax H3 的应用镜像
（MINIMAX-H3提速500高画质 v9.26，权重全带、零下载），通过 ComfyUI 原生 API 跑通了
**H3 Ref2VA（多图参考）视频生成**，两条都带原生音频。

### 验证结果

| 测试 | 参考输入 | 耗时 | 输出 |
|---|---|---|---|
| 单图参考 | 1 张角色图 | ~90s | 864×480 / 5.17s / h264+aac |
| 双图参考 | 2 张角色图 | ~3min | 864×480 / 5.17s / h264+aac |

### 关键链路

```
本地参考图 → POST /api/comfy/upload/file → 拿 name
→ 构造 R2V 工作流 JSON（MiniMaxH3ReferenceToVideo 节点）
→ POST /prompt 提交 → 轮询 /history → GET /view 下载视频
```

---

## 二、本次操作避坑清单（血泪）

### 1. AutoDL 平台层
- **国内访问 www.autodl.art 不要挂代理** —— 挂了代理某些接口/JS 抓不到，直连即可
- **钱包余额**：`POST https://api.autodl.com/api/v1/dev/wallet/balance`（注意 host 是
  `api.autodl.com` 不是 `autodl.art`，autodl.art 上没有钱包接口）。`assets` 单位是厘，÷1000=元
- **CLI 只有实例管理命令**（create/status/info/list/stop/rm），没有模型调用命令；
  但 autodl-cli 就是够用的，模型生成走实例内 ComfyUI
- **GPU 规格选择**：H3 需要大显存。v-48g 无库存、pro6000-p 余额不够（¥7+/时）、
  5090-p 服务忙 → 最后 **v-32g-p（¥1.77/时）能用**，配 int8/量化权重跑 H3
- **`create --dry-run` 只回显参数不报价格**，价格要 `info` 看 `payg_price`（单位厘）
- 系统盘扩容：H3 权重在公共库 symlink，`--disk 80` 预留足够

### 2. 镜像使用层
- **权重全预装，零下载**：Ref2VA/FL2VA 的 UNet、qwen3vl 编码器、双 VAE 全在
  `/.autodl` 公共库 symlink 指向，不占盘。这个镜像（MINIMAX-H3提速500）确实"数据模型都有"
- **镜像自带面板 API**（`/api/workflow/generate`）在 6008 端口（外网 8443），
  但 U02 自带工作流会报 `147(MiniMaxLowVRAMAttention): Required input is missing`
  —— **镜像面板的 /api/workflow/generate 校验不过**，别用它
- **正解**：绕过面板，用 **ComfyUI 原生 API（6006 端口）**。SSH 隧道
  `ssh -fN -L 8190:127.0.0.1:6006` 后 `POST /prompt` 直接提交自定义工作流
- 外网端口 8443 是 HTTPS，`curl` 要 `-k`；直接 http 访问报
  "400 plain HTTP sent to HTTPS port"

### 3. H3 工作流 / 节点层（重点）
- **U02 图生视频工作流加载的是 FL2VA 模型**（首尾帧），不是 R2V；要"多图参考"
  必须用 **`MiniMaxH3ReferenceToVideo`** 节点 + Ref2VA UNet
- **R2V 节点的 autogrow 参数命名坑**：execute 签名是 `ref_images`（复数 dict），
  不是 `ref_image`。API 格式下要传：
  ```json
  "ref_images": {"ref_image_1": ["1",0], "ref_image_2": ["16",0]}
  ```
  传 `ref_image`（单数）会报 `TypeError: got an unexpected keyword argument 'ref_image'`
- **R2V 需要双 VAE**：`video_vae` + `audio_vae` 都要接，否则没有音频输出
- **ComfyUI 原生 /prompt 的 body 要包一层**：`{"prompt": <workflow>, "client_id": "xx"}`，
  直接传 workflow 会报 `no_prompt`
- **轮询要长**：多图参考 ~3min，单图 ~90s。`/history` 在执行完前是空的（不是报错），
  看 `/queue` 里 `queue_running` 判断"还在跑"更直观
- **保存视频节点**：SaveVideo + CreateVideo(fps=24)，输出走 `GET /view?filename=...&subfolder=video&type=output`
- 模型选型（32G 显存）：`minimax_h3_ref2va_pruned_int8_convrot.safetensors`
  + `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` + 双 VAE

### 4. H3 提示词（多图参考的核心）
- prompt 里用 **`<Picture 1>` / `<Picture 2>`** 引用各参考图，模型才理解"这两张图是主体"
- 多图参考是 **Ref2VA** 模式，提示词要写结构化剧本：`[Shot 1]` 场景/动作/运镜 +
  时间轴 `[Shot 2] At 00:04.000` + `overall_soundscape` + `non_diegetic_music`
- 角色一致性靠：**每镜头重复主体描述 + <Picture N> 引用**（和以前 LLM 分镜的
  "角色描述跨镜头重复"写法同源，但现在有真图做锚）

---

## 三、费用

- 实例 vGPU-32G ¥1.77/时，两条视频全程约 20 分钟 ≈ **¥0.6**
- 钱包余额 ¥2.85 起步，结束约 ¥2.2

---

## 四、遗留 / 下一步

- ✅ 链路验证通过（单图 + 双图参考都出带声音视频）
- ❌ **双图测试角色脸不像**（用户实测反馈，2026-08-19）——参考图是 `char_ref_00001/00002`，输出女主脸与参考图不一致。这是本次最关键的失败反馈，见下方「一致性失败分析」
- ⬜ 还没测 9 图极限参考、视频/音频参考输入
- ⬜ 角色一致性量化对比（参考图 vs 输出帧）没做，预览帧在 `output/multi_frame_preview.png`
- 后续可把 `batch_generate.py` 管线改造成"分镜 → H3 R2V 批量出片"

### 一致性失败分析（脸不像，为什么）

| 可能原因 | 影响 | 本次配置 |
|---|---|---|
| `ref_image_size="match"` | 参考图按生成尺寸(864×480)等比缩小，**身份细节丢失**；`max` 模式用 2048px 短边，身份保真度更高 | match ← 可能主因 |
| 参考图张数只有 2 张 | 角度/光线信息不足，身份特征不稳定 | 2/9 |
| 参考图本身是 AI 生成 | 24 张 char_ref 是文字生成图，**脸本来就有漂移**（L4 遗留问题），做锚时继承了这个漂移 | 已用 |
| 量化精度 | int8 UNet + nvfp4 编码器对身份细节有损 | int8/nvfp4 |
| prompt 未强锁脸 | 只写"combining appearance shown in Picture"，没下"脸必须严格来自"的强指令 | 弱 |
| steps=25 偏低 | 细节收敛不足 | 25 |

**改进方向（下次验证优先级）**：
1. `ref_image_size` 改 `max`（最高优先，官方明确说身份保真用 max）
2. 参考图加到 4-9 张（不同角度/光线/表情），或换用更"正脸、清晰"的参考图
3. prompt 加强锁脸指令：`Her face must be EXACTLY the face in <Picture 1>, no alteration`
4. 试更高分辨率（如 1344×768）或更多 steps
5. 若仍不像：考虑 H3 LoRA（镜像有 Ai-toolkit H3 训练环境），或参考图用更高质量照片级图
